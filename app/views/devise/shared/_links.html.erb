<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= link_to fa_icon('gitlab', text: t("buttons.sign_in_with_#{provider}")), omniauth_authorize_path(resource_name, provider), class: 'btn btn-success btn-block' %><br />
  <% end -%>
<% end -%>
