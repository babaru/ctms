<%= simple_form_for(@project, html: { class: 'form-horizontal' } ) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, label: t('activerecord.attributes.general.name') %>
  <div class="hr-line-dashed"></div>

  <div class="form-group">
      <div class="pull-right m-r-md">
          <%= f.submit t('buttons.save'), class: 'btn btn-primary btn-w-m' %>
      </div>
      <div class="m-l-md">
        <%= link_to t('activerecord.text.back_to_list', model: Project.model_name.human), projects_path, class: 'btn btn-white' %>
      </div>
  </div>
<% end %>
